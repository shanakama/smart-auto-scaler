# Core dependencies
flask==3.0.0
flask-cors==4.0.0
kubernetes==28.1.0
numpy>=1.24.0
gunicorn==21.2.0
requests>=2.31.0

# PyTorch - CPU only (much smaller and faster to install)
# Use extra-index-url to add PyTorch repo while keeping PyPI
--extra-index-url https://download.pytorch.org/whl/cpu
torch
